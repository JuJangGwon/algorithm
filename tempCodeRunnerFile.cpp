      // if(q.empty())
        //     break;
        // int x = q.front();
        // q.pop();
        // result[i] = x;
        // cout << x;
        // for (int j = 0; v[i].size();j++)
        // {  
        //   //  int y = node_N[v[i][j]]--;
        //   //  if (y == 0)
        //   //      q.push(v[i][j]);
        // } 