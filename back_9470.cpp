#include <iostream>
#include <queue>
#include <vector>

vector<int> v[1001];

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int a;
    cin >> a;
    for (int i = 1; i <= a; i++)
    {
        int a, b, c;
        cin >> a >> b >> c;
    }
}