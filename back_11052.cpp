#include <iostream>
#include <algorithm>

using namespace std;

int a;
int map[1001];
long long dp[1001];

int main()
{
    cin >> a;
    for (int i =1 ;i <= a; i++)
    {
        cin >> map[i];
    }
    dp[1] = map[1];
    for (int i = 1; i <= a; i++)
    {
        for (int j = 1; i <= i; i++)
        {

        }
    }
    
}